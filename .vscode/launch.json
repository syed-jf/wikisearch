{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run agent",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/agent.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "preLaunchTask": "Build agent (g++)",
      "MIMode": "gdb",
      "miDebuggerPath": "gdb"
    }
  ]
}
